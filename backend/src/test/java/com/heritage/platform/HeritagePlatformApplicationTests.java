package com.heritage.platform;

import org.junit.jupiter.api.Test;

/**
 * Smoke test for the Heritage Platform application.
 *
 * <p>Verifies that the test infrastructure compiles and runs correctly.
 * A full Spring context load test will be added once database and security
 * configuration are in place.
 */
class HeritagePlatformApplicationTests {

    @Test
    void contextLoads() {
        // Intentionally empty — verifies the test infrastructure works.
        // Full Spring context load test will be added once database and
        // security configuration are in place.
    }
}
